// import { expect } from 'chai';
// import { mount } from '@vue/test-utils';
// import BlogComponent from '../../../src/views/Blog';

xdescribe('BlogComponent', () => {
  xdescribe('The template', () => {
    xit('renders a containing div', () => {
    });

    xit('renders a list of all posts', () => {
    });

    xit('renders a list of posts for the current user', () => {
    });
  });

  xdescribe('The lifecycle', () => {
    xdescribe('on created', () => {
      xit('fetches a list of all posts', () => {
      });

      xit('fetches a list of all users', () => {
      });
    });
  });
});
